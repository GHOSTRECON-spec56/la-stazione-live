{
  "name": "la-stazione-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/check-build.mjs",
    "dev": "node tools/dev-server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "google-auth-library": "^10.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "qrcode": "^1.5.4"
  }
}
